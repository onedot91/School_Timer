# 로딩 지연 점검 (2026-09-07)

## 운영 측정

학생 데이터 변경 없이 임시 기기 쿠키를 발급하고 GET 요청만 측정했다. 응답 내용과 쿠키는 측정 파일에 저장하지 않았다.

| 항목 | 측정 결과 |
| --- | --- |
| 학생 설정 GET 3회 | 1,303 / 892 / 878ms, 각 113,436 bytes |
| 변경 시각 GET 3회 | 449 / 865 / 213ms |
| 실행 경로 | 응답 `x-vercel-id`: `icn1::iad1` |
| Supabase 위치 | 서울 (`ap-northeast-2`) |
| 대표 설정 SELECT | PK 인덱스 사용, 실행 9.681ms (전체 API 쿼리와는 별도 측정) |

DB 실행 시간만으로 전체 지연을 설명할 수 없으며, API가 미국 동부에 있고 DB가 서울에 있는 왕복 경로가 확인됐다.

## 적용한 변경

- `vercel.json`의 함수 지역을 서울 `icn1`로 지정했다. 2026-09-07 운영 배포에 반영되었다. [Vercel 지역 설정](https://vercel.com/docs/functions/configuring-functions/region)
- 같은 캐시 세대에서 동시에 발생하는 설정 GET을 한 요청으로 합친다. 완료된 응답을 임의의 TTL로 재사용하지 않는다. 저장·무효화 이후 조회는 이전 조회와 분리한다.
- 조회는 요청/본문 전체 12초 제한, 재시도 1회로 변경했다. 기존 조회 최악의 대기는 45초 × 3회였다. 저장은 기존 45초 제한과 조건부 요청·저장 결과 확인을 유지한다.
- 교사 설정의 낱말판·오늘의 친구·책방 챌린지·글쓰기 패널은 열 때 내려받는다.

## 로컬 검증

실제 API를 합성 응답으로 대체한 Chrome, 1280×650, CPU 4배 제한, 캐시 없는 별도 페이지에서 비교했다.

- 교사 초기 JavaScript: 1,198,715 → 817,317 bytes (약 31.8% 감소, 압축 해제 기준).
- 학생 초기 JavaScript: 620,930 → 621,574 bytes. 학생 화면의 주요 개선 대상은 API 경로와 조회 대기다.
- 로컬 첫 화면 시간은 이번 측정에서 개선이 입증되지 않았다. 파일 크기 감소를 실제 운영 로딩 시간 감소율로 해석하지 않는다.
- 교사 4개 패널 진입 및 학생 첫 화면에서 JavaScript 오류 없음.
- 전체 테스트 894개, 타입 검사, 프로덕션 빌드 통과.
- 동시 조회 8회 → 네트워크 요청 1회, 다음 조회는 최신값 조회.
- 캐시 무효화·저장 완료·손상된 저장 영수증 뒤에는 오래된 배경 응답을 재사용하지 않는 회귀 검증.
- 응답이 멈춘 조회 종료와 다음 조회 복구 검증. 운영 학생 잔액·기록에 쓰기 검증하지 않음.

## 운영 배포 후 검증

애플리케이션 커밋 `786dccef99170288e0c2044b29df79f350277ed0`의 Vercel 배포 성공을 확인한 뒤, 같은 GET 요청을 3회씩 다시 측정했다.

| 항목 | 변경 전 중앙값 | 변경 후 중앙값 | 개선 |
| --- | ---: | ---: | ---: |
| 학생 설정 GET | 892ms | 164ms | 약 82% 감소 |
| 변경 시각 GET | 449ms | 59ms | 약 87% 감소 |

- 설정 GET 개별 결과: 165 / 142 / 164ms. 모두 HTTP 200, 이전과 동일한 113,436 bytes.
- 변경 시각 GET 개별 결과: 45 / 176 / 59ms. 모두 HTTP 200.
- 응답 실행 지역: `icn1::iad1` → `icn1::icn1`.
- 배포된 정적 자산으로 학생 첫 화면·교사 화면·교사 4개 설정 패널을 Chrome에서 열어 JavaScript 오류 0개를 확인했다. 이 화면 검증에서는 모든 API를 합성 응답으로 대체했다.
- 기존 학생 기록을 변경하는 요청은 실행하지 않았다. 세션 발급은 쿠키만 생성하고 이후 GET만 호출했다.
- 표의 개선율은 이 환경에서 측정한 API 응답 시간이다. 학생 크롬북의 화면 전체 로딩 시간이나 모든 저장 작업의 개선율을 의미하지 않는다. 학교 Wi-Fi와 기기 성능에 따른 차이는 남을 수 있다.

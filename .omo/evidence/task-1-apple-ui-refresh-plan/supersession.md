# Supersession

`baseline-results.json`, `deadline-path-results.json`, and `screenshots/` are the canonical Todo 1 evidence set. `baseline-results.json` is the only successful canonical baseline; `deadline-path-results.json` is its intentionally failing deadline-path proof.

The preexisting `repeat-run-1-results.json`, `repeat-run-2-results.json`, `selected-confirmation-recovery-results.json`, and `recovery-screenshots/` are historical, non-canonical artifacts. They must not be combined with the current 11-scenario matrix.

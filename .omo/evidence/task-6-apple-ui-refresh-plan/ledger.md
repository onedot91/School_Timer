# Todo 6 Timer overlay ledger

Authoritative evidence: `complete-current.json` and `complete/authoritative-todo6-baseline.json`.

| Surface | Taxonomy | Runtime receipt |
| --- | --- | --- |
| Settings | Parent modal | `timer/settings-shell`; green Backup focus; responsive and preference matrix |
| Announcement | Full-screen task modal | `timer/announcement-default`; exact trigger return; top input focus effect absent |
| History | Nonmodal child drawer | `timer/announcement-history`; static open-close-open geometry |
| Memo | Full-screen task modal | `timer/memo`; contenteditable participates in Tab/Shift+Tab trap |
| Currency | Anchored nonmodal pane | personal, invalid, all, synthetic-fixture feedback; no live mutation |
| YouTube | Anchored nonmodal pane | ready, empty, error; API key empty and external requests blocked |
| Library | Anchored/fixed responsive pane | ready, loading, unavailable; desktop and fresh mobile pixel audits |
| Question | Anchored nonmodal pane | submitted/loading/error/empty with corrected isolated endpoint schema |
| Award confirm | Settings child modal | one top modal; parent inert+aria-hidden; exact 낙찰 return |
| Award presentation | Settings child modal | timed Escape blocked; parent ownership restored |
| Reset confirmation | Settings child modal | exact 주간 경매 마감 return |
| Standalone award | Top modal | settings absent; effective inert background; exact 알림장 return |
| Embedded draw | Transient noninteractive presentation | reset/rolling/winner/repeat; motion surfaces excluded only during named impact frames |

## Matrix and invariants

- 57 states, 456 viewport records: 320×568, 390×844, 768×1024, 1024×768, 1280×720, 1440×900, 1280×600, and 390×844 at 200%.
- Required IDs have no missing, duplicate, or unexpected entries.
- All normal-scale stable surfaces remain inside the viewport. At 200%, vertical scrolling is allowed while horizontal overflow and 44px targets remain enforced.
- The only surface-clipping exclusions are named draw impact/rolling frames and the standalone decorative draw board; page overflow and controls remain enforced.
- Baseline repairs do not use current-to-current copy claims: repaired strings are proven against pristine git source `f5a90d8`, while the question data shape is proven by the corrected isolated endpoint fixture.
- Non-local requests are stored only as redacted categorical labels. No credential-bearing URL is retained.
- Actual classroom balances, bids, awards, histories, and shared settings were never mutated.

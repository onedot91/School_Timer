# Task 3 carried-book Canvas cue fix

## Change

`src/components/student/library/CanvasLibraryRenderer.ts:607-612` now chooses the placed-book cue from the actual scene state:

- registration desk: `E  책 등록`
- empty hand + placed book: `Enter  책 보기`
- carrying a draft + placed book: `E  책장 열기`
- shelf: `E  책장 열기`

The only product edit is the `drawCue` label conditional. No palette, layout, interaction, route, backend, parser, or dependency changed.

## Before / after source binding

- Before edit SHA-256: `baf3fa7b5a93b6984b68d862fc0a937821464af9a47d76ac1312df65258705a0`
- After edit SHA-256: `b21940d721ed9c9062f78c21939b65299e8113f7a555f599629326895de73ca7`
- Hash receipt: `task-3-cue-fix-sha256.txt`

The authoritative RED capture was `.omo/evidence/canvas-library/pointer-carrying-near-placed.png`: it showed `Enter  책 보기` while the DOM action was `가까운 곳 살펴보기: 책장 열기` and `interact` opened the shelf picker. The fresh post-edit capture `.omo/evidence/canvas-library/root-carrying-near-placed.png` is a valid `PNG image data, 1280 x 800` and visibly shows `E  책장 열기` above the placed-book target while the bear carries a book.

## Verification

| Success criterion | Exact invocation / scenario | Binary observable | Artifact |
| --- | --- | --- | --- |
| TypeScript remains valid | `npm run lint` | exit code `0` (`tsc --noEmit`) | `task-3-cue-fix-lint.log` |
| World behavior regression-free | `node --import tsx --test src/lib/canvasLibraryWorld.test.ts` | `14` passed, `0` failed, exit code `0` | `task-3-cue-fix-world-test.log` |
| Patch has no whitespace errors | `git diff --check` | exit code `0`; empty output | `task-3-cue-fix-diff-check.log` |
| Carried draft near an occupied placed-book cue | HMR-disabled browser surface run at viewport `1280×800`; actual game flow reached `placed-book:0` with `carriedDraft` and captured Canvas | fresh PNG signature and dimensions verified by the browser fixture; visible label is `E  책장 열기` | `root-carrying-near-placed.png` |
| Existing normal empty-hand cue remains unchanged | Same renderer branch plus the fresh actual game loop in `root-play-qa.mjs` reaches an occupied placed-book target after placement with `carriedDraft` cleared and presses `E` to open its details | branch resolves to `Enter  책 보기`; actual `E` outcome is the details dialog; no other cue branch changed | `src/components/student/library/CanvasLibraryRenderer.ts`, `root-play-qa.json` (`passed: true`, `actualBookLoop: true`), and `root-details.png` |

The fresh HMR-disabled browser receipt was generated by `node .omo/evidence/canvas-library/root-play-qa.mjs` at `2026-09-05T06:30:05.377Z`: `passed: true`, viewport `1280×800`, `actualBookLoop: true`, `errors: []`, `blockedRequests: []`, and the renderer hash equals the after-edit hash above. Its 16 listed captures include the visible carried cue at `root-carrying-near-placed.png`; the empty-hand placed-book path is also exercised before `root-details.png` by the real `E` interaction.

## Cleanup

The browser fixture closes its owned Chrome context. I briefly started Vite on `127.0.0.1:3025` for a preview attempt; cleanup receipt: `kill 89057` exited `0`, and `lsof -nP -iTCP:3025 -sTCP:LISTEN` reported no listener. No dependency, data write, or external asset was created. Parent-owned Vite/browser verification was separately closed by the parent after the passing run.

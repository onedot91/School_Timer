# 2026-09-09 입찰 실패 조사

## 운영 관측

- 12:44:18 KST `/api/shared-settings` POST 403. 응답 본문과 학생 식별자는 로그에 없어 사용자 화면과 동일한 요청인지는 확정하지 못했다.
- 12:42~12:46 KST 학생 8명의 입찰 16건이 저장 영수증에 존재한다. 모든 학생에게 항상 실패하는 상태는 아니지만, 개별 실패가 없다는 뜻은 아니다.
- 운영 입찰·잔액 변경으로 테스트하지 않았다.

## 재현 및 수정

- RootApp은 교사 세션으로 학생 화면을 열도록 허용하지만 기존 저장 명령은 교사 세션의 학생 입찰을 `TEACHER_COMMAND_REQUIRED` 403으로 거절했다.
- 격리 API 테스트에서 학생 23명의 순차 입찰 성공 후 교사 학생 화면 입찰만 403으로 실패하는 것을 재현했다.
- 입찰 클라이언트는 선택한 학생 번호를 명시한다. 서버는 학생 세션이면 본인 번호만 허용하고, 교사 세션의 해당 번호 지정은 `student.auction.bid`에만 허용한다.
- 저장 영수증은 `teacher:0:student:<number>`로 분리한다. 다른 학생 번호와 학생 세션에서는 해당 영수증을 공유하지 않는다.
- 저장 응답 미확인과 재시도에서도 동일한 학생 번호와 요청 ID를 유지한다. 다른 학생 데이터나 잔액을 쓰지 않는다.
- 권한 거절·확인 대기·입찰 조건 오류의 안내를 구분한다.

## 검증 범위

- 수정 후 로컬 HTTP에서 학생 23명의 입찰, 교사 학생 화면 입찰 200 확인. 다른 학생 번호를 지정한 학생 요청 403 확인.
- API/클라이언트 회귀 테스트에서 번호 검증, 다른 기능의 교사 대리 실행 거절, 학생별 영수증 격리, 동일 요청 재시도를 확인했다.
- 이 수정으로 모든 운영 실패의 원인이 밝혀진 것은 아니다. 정상 학생 세션에서 발생한 추가 실패는 실제 응답 코드가 필요하다.

## 운영 반영

- 사용자 승인 후 2026-09-09 13:15 KST 운영 전환 완료.
- 배포: `dpl_8zqjHZWWUkfPcJ3fnGnGGd8YcFGJ`.
- 릴리스 소스 해시: `d351fcfbb21997fc65c5c2d847e313f8006f932ed292e110988ad2e7677cb988`.
- 전체 릴리스 게이트 통과: 타입 검사, 기본/API 테스트, 서버 테스트, 저장소/보상/Classword SQL, HTTP, 빌드된 서버 실행, 복구 훈련, 프로덕션 빌드.
- 전환 전 새 배포와 전환 후 운영 도메인에서 교사/학생 metadata 및 snapshot 조회 모두 200. 학생 데이터 범위 유지와 실행 리전 `icn1` 확인.
- 운영 전환 후 metadata 91~121ms, snapshot 797~922ms. 실제 학생 입찰·잔액은 QA로 변경하지 않았다.
- 작업 트리의 수정 파일은 커밋하지 않고 검증된 복사본을 배포했다.

# 경매 물품 누락 복구 검증 (2026-09-15)

## 확인한 원인

- 운영 저장 영수증과 리소스 수정 시각에서 12:31 KST `teacher.settings.patch` 이후 기존 경매 물품이 기본 목록으로 바뀐 것을 확인했다. 조회 최적화 SQL 적용(12:35 KST)보다 앞선 변경이다.
- 최초 설정 GET 실패에도 초기화 완료로 표시하던 코드 때문에, 다시 확인 과정에서 빈 저장 기준과 기본 화면의 차이가 사용자 편집으로 취급됐다. 실제 콜백을 실행한 실패 재현에서 기본 경매 목록을 저장하는 변경이 생성됐다.
- GET 성공 전에는 공유 저장을 시작하지 않도록 수정했다. 복구 시 서버 설정과 실제 편집만 병합하며, 초기 조회 실패 중 편집은 별도 로컬 초안으로 보관한다.
- 서버의 일반 설정 저장은 기존 물품 삭제, 등록 플래그 제거 및 구형 이름 있는 물품의 기본 항목 전환을 거부한다. 전용 물품 삭제 경로와 정상 이름 변경은 유지한다.

## 운영 복구 범위

`dev/recover-auction-items-20260915.sql`을 기존 원자적 저장 RPC로 실행했다. 원본과 삭제 시각, revision이 확인된 두 항목만 대상으로 삼았다.

- `item-2-2`: 랜덤 지우개 뽑기 1회
- `item-2-3`: 수제꿀 오란다

복구 영수증 `restore-auction-items-20260915-033100`의 완료 시각은 2026-09-15 03:55:19 UTC이다. 복구 후 범위 조회에 두 항목이 포함됐고, 해당 작업의 잔액 원장 변경은 0건이었다. 다른 오래된 삭제 항목을 일괄 복구하지 않았다. 원본을 확인하지 못한 다른 누락 물품은 복구됐다고 단정할 수 없다.

## 검증

- 실제 PostgreSQL 및 HTTP 통합 검사: 정확한 두 항목만 복구, 원본 revision 변경 시 중단, 재실행 중복 방지, 다른 리소스·지갑 보존, 교사와 학생 GET 모두 통과.
- 격리된 브라우저/서버: 최초 GET 502 실패 → 설정의 저장 다시 확인 → 기존 두 항목 표시. 이름 변경 저장 후 새로고침해도 유지되고 학생 경매장에서도 동일하게 표시됨.
- 서버 보호 회귀 검사: 기본값 덮어쓰기 실패 재현 후 수정, 정상 편집과 전용 삭제 허용 포함 17개 통과.
- 최종 빌드의 실제 브라우저 검사: 최초 GET 장애 중 월요일 물품 이름 편집 → 새로고침 → 연결 복구 → 저장 다시 확인. 기존 화요일 물품 2개와 보관한 월요일 편집이 모두 표시됐다. 장애 중 서버 쓰기는 0건이었고 복구 후 실제 편집만 저장됐다. 콘솔에는 의도적으로 발생시킨 조회 실패가 기록됐으며 복구 후 새 예외는 없었다.
- 관련 회귀 검사 52개 통과(클라이언트 35개, 서버 17개). 독립 검토도 통과했다.
- 전체 `npm test`: 1,316개 중 1,313개 통과, 1개 건너뜀, 기존 편지 검사 2개 실패 (`tests/api/storageCommands.test.ts:67,73`, 학생 수 23/25 기대값 차이).
- `npm run build` 통과. `npm run lint`는 기존 `src/lib/studentPet.test.ts:204` TS2740 오류로 실패했다. 변경 파일의 신규 타입 오류는 없으며 `git diff --check` 통과.

## 적용 상태

두 항목의 운영 데이터 복구는 완료했다. 재발 방지 소스는 로컬 수정이며 Netlify 배포는 사용자가 수행해야 한다. 이번 경매 수정은 운영 동시 접속 용량 전체가 검증됐다는 뜻이 아니다.
